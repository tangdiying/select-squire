import { SelectDataDirective } from './select-data.directive';

describe('SelectDataDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectDataDirective();
    expect(directive).toBeTruthy();
  });
});
